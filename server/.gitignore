# Local, secret, or generated — never commit these.
.env
api/appsettings.Production.json
client/appsettings.Production.json
certs/*.crt
certs/*.key

# Runtime + persisted content (your plugin bundle / tenant config / secrets / signing keys).
config/*
plugins/*
secrets/*
api/home/*
client/home/*
!**/.gitkeep

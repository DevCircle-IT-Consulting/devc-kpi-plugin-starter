{
    "//": "Copy to server/client/appsettings.Production.json. These values are served to the browser via /Config and MUST match the API's Identity URLs and the address you actually browse.",
    "ApiUrl": "https://localhost:8443/api/",
    "IdentityUrl": "https://localhost:8443/api/",
    "AllowedHosts": "localhost"
}

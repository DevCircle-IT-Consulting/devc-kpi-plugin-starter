# The SalesDemo plugin is ForTenants("salesdemo") so it auto-binds - no opt-in needed here.
# This file is kept only to show where Shared-plugin opt-ins go (see the template's plugins.yaml).
enabled: []
